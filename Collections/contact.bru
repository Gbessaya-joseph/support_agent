meta {
  name: contact
  type: http
  seq: 16
}

post {
  url: {{backend_base_url}}/api/v1/contact
  body: json
  auth: inherit
}

body:json {
  {
    "email":"{{test_email}}",
    "name":"{{test_name}}",
    "message":"{{test_message}}"
    
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
